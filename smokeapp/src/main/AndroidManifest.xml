<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_appsmoke"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_appsmoke_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AdvanceDevApp">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ActivityRegister" android:exported="true"/>
        <activity android:name=".ActivityPanel" />


    </application>

</manifest>